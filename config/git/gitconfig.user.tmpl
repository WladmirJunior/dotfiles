[user]
    name = ${GIT_NAME}
    email = ${GIT_EMAIL}

# @if IS_WORK_MAC
[commit]
    gpgsign = false
# @endif

# @if IS_WORK_MAC == "0"
[push]
    autoSetupRemote = true
[pull]
    rebase = true
# @endif
